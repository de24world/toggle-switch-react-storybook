import "./styles.css";
import ToggleButton from "./ToggleButton";

export default function App() {
  return (
    <div className="App">
      Exist Error, see only Code must fix size & onToggle
      <ToggleButton />
    </div>
  );
}
